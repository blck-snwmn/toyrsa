pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.go"
      run: golangci-lint run --enable=gosec
    test:
      glob: "*.go"
      run: go test ./... --shuffle on --parallel 10 --p 10
